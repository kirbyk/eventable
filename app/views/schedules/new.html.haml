.col-lg-4.col-md-offset-4
  .panel.panel-default
    .panel-heading
      %h3.panel-title Add a new schedule item to #{@event.name}
    .panel-body
      = render 'form', submit_verb: 'Add'

:javascript
  $(document).ready(function() {
    $('#start_time, #end_time').datetimepicker();
  });
