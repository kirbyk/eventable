- if flash[:notice]
  .flash
    =flash[:notice]
	
.col-lg-3
.col-lg-6.mid
  - if @updates.count > 0
    .div.title
      = "#{pluralize @updates.count, "update"} in #{@event_name}"
  - else
    .div.title
      = "No update for this #{@event_name}"
  .div
    = link_to "Create new update", controller: "updates", action: "new", event_id: @event_id
  - @updates.each do |u|
    = form_for(u, :method => :delete, :id => u.id) do |f| 
      = tag("input", type: 'hidden', name: "event_id", value: @event_id)	  
      .schedule
        = simple_format u.description
        .schedule_time.margin
          = "#{u.created_at.strftime("%m/%d/%Y")}"
        .actions.margin 		
          = f.submit "delete"
        .margin
          = link_to "Edit", controller: "updates", action: "new", event_id: @event_id, update_id: u.id, edit: true
        